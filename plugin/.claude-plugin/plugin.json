{
  "name": "glassboard",
  "version": "1.0.0",
  "description": "Accessible web viewer for Claude Code sessions with real-time updates, sounds, and notifications",
  "author": {
    "name": "Akash Kakkar"
  },
  "settings": {
    "autoOpen": {
      "type": "boolean",
      "default": true,
      "description": "Automatically open Glassboard in the browser when a session starts"
    },
    "stopOnSessionEnd": {
      "type": "boolean",
      "default": false,
      "description": "Stop the Glassboard server when the Claude session ends"
    },
    "port": {
      "type": "number",
      "default": 4001,
      "description": "Port for the Glassboard server"
    },
    "glassboardPath": {
      "type": "string",
      "default": "",
      "description": "Path to the Glassboard project directory. Leave empty to use the plugin's bundled copy."
    }
  }
}
